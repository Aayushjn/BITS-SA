spring:
  datasource:
    url: jdbc:h2:mem:db
    driver-class-name: org.h2.Driver
  mvc:
    problemdetails:
      enabled: true
travelms:
  services:
    hotels: travelms-hotels:8080
    buses: travelms-buses:8080